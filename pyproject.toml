[project]
name = "back-hackaton-ctw-2026"
version = "0.1.0"
description = "Django + PostgreSQL backend"
requires-python = ">=3.12"
dependencies = [
    "django>=5.0,<6.0",
    "psycopg2-binary>=2.9",
    "python-dotenv>=1.0",
]
